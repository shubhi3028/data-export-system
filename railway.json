{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "cd backend && go build -o api ."
  },
  "deploy": {
    "startCommand": "cd backend && ./api"
  }
}
